# Agentic-Federated-Lexical-RAG
Agentic Federated Lexical RAG is a Vectorless RAG solution which guarantees 100% data freshness, zero data duplication, native security compliance (since the API is queried on behalf of the user), and significantly higher accuracy than naive Vector RAG.
